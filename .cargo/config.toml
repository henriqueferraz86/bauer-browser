# Local: redireciona target/ para fora do Documents (evita bloqueio WDAC)
# Em produção (GitHub Actions), CARGO_TARGET_DIR do workflow sobrescreve isso.
[build]
target-dir = "C:\\Users\\henri\\AppData\\Local\\cargo-target\\bauer-browser"
